const initialShop = {
  isLoggedIn: false,
  shopId: 115,
};

const shopReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { initialShop, shopReducer };
