const initialAuth = {
  isAppStartUp: true,
  isSignedIn: true,
  authToken: "",
};

const authReducer = (state, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export { initialAuth, authReducer };
